# Railway Environment Variables
# Configure estas variáveis no Railway Dashboard -> Service -> Variables

# Basic configuration
NODE_ENV=production
PORT=3001

# Database Configuration - Use componentes separados para segurança
# NÃO adicionar senha diretamente na DATABASE_URL
DB_HOST=db.your-supabase-project.supabase.co
DB_PORT=5432
DB_NAME=postgres
DB_USER=postgres
DB_PASSWORD=your-secure-password-here
DB_SSL=true

# Supabase Configuration (RECOMENDADO)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# A DATABASE_URL será construída automaticamente no código
# DATABASE_URL será gerada como: postgresql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}?sslmode=require

# Railway configuration (setadas automaticamente pelo Railway)
# RAILWAY_ENVIRONMENT=production
# RAILWAY_PROJECT_ID=auto-set-by-railway
# RAILWAY_SERVICE_ID=auto-set-by-railway
# RAILWAY_DEPLOYMENT_ID=auto-set-by-railway

# Optional: Prisma configuration
PRISMA_CLI_QUERY_ENGINE_TYPE=binary

# Optional: Custom healthcheck timeout
RAILWAY_HEALTHCHECK_TIMEOUT_SEC=300
